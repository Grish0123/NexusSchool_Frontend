# Local dev can stay empty because Vite proxies /api to http://localhost:3000.
VITE_API_BASE=

# Production example, when the backend is hosted on a separate cPanel Node app/subdomain:
# VITE_API_BASE=https://api.nexus.edu.np
